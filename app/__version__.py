# app/__version__.py
"""앱 버전 (Semantic Versioning). 릴리즈마다 갱신한다.

1.0.0 = PLAN.md Phase①~⑥(시뮬레이터 기반 개발) + PyInstaller 패키징까지 완료된
첫 배포 가능 상태 (2026-07-29, git tag v0.8-pyinstaller 시점).
1.1.0 = Phase③ 실장비 검증 1차 피드백 반영 — Poly SSH 트랜스포트 추가,
장비 등록 시 즉시폴링 제거, 죽은 사이드바 링크 제거, 파일 로깅 추가.
1.2.0 = Teams 수동 다이얼(CVI 회의ID+테넌트 직접 다이얼) + 장비 카드 v2
(모델/가동시간 자동표시, 아이콘 제어버튼, Teams 오늘회의 목록, 장비 수정 모달).
1.2.1 = Phase③ VDI 실장비 검증 — Poly 실장비가 ssh-rsa(SHA-1)만 지원하는
호스트키 문제로 SSH 연결이 전부 실패하던 버그 수정.
1.3.0 = `/logs` 페이지에 제어 로그/시스템 로그 탭 분리 — data/app.log를
UI에서 조회 가능(신규 /logs/system 라우트).
1.4.0 = 제품 브랜드를 "BridgeX"로 확정, coredxi 사내 로고를 헤더/파비콘/exe
아이콘에 통합. 16px 파비콘 전용으로 육각 체인 디테일을 단순화한 버전 별도 생성.
1.5.0 = 미래 BridgeX 고객을 대상으로 한 앱 내장 사용자 가이드(`/guide`) 추가 —
사이드바 맨 아래에 링크. 긴 페이지에서 사이드바가 본문 높이만큼 늘어나던
레이아웃 버그를 이번에 함께 수정(`align-self: flex-start` + 고정 높이 + sticky).
1.5.1 = 로컬 포트(8765) 충돌 시 자동으로 다음 포트(8766~8769)를 시도하도록
run.py 수정 — 기존에는 다른 프로그램이 8765를 쓰고 있으면 서버가 조용히
죽고 브라우저만 열려 "사이트에 연결할 수 없음"만 뜨던 문제(BACKLOG.md).
1.5.2 = 배포 exe 파일명을 "CodecControlCenter-vX.Y.Z"에서 "BridgeX-vX.Y.Z"로
변경(build.spec) — 가이드(/guide)가 이미 이 이름으로 안내하고 있어 실제
파일명과 맞춤. 가이드 화면에서 스크롤 시 화면이 튀던 버그도 함께 수정
(사이드바 position: sticky 제거 — .topbar가 sticky가 아닌 상태에서 사이드바만
sticky로 고정한 게 원인).
1.5.3 = 2026-07-31 VDI 1차 실장비 필드 테스트에서 발견된 버그 2건 수정.
(1) Cisco Bookings List 응답에 "*r BookingsListResult " 프리픽스가 매 줄에
붙는다는 걸 실장비 원문으로 확인 — 기존 파서가 이를 고려하지 않아 Teams
일정이 하나도 안 잡히던 버그(cisco_driver.py `_parse_bookings_list`).
(2) 수동 새로고침(poll_once)이 배경 폴링(_poll_loop)과 같은 장비의 텔넷/SSH
연결을 동시에 건드릴 수 있어(세마포어는 전체 동시접속 수만 제한, 장비별
배타 제어 없음) 응답 줄이 뒤섞이며 통화중 상태가 영구히 잘못 표시되던 버그 —
PollingScheduler에 장비별 asyncio.Lock 추가, 제어 API도
`run_with_driver()`로 전환해 같은 락을 공유하도록 수정.
1.5.4 = 2026-07-31 VDI 1차 필드 테스트 2차 피드백 반영.
(1) Cisco mute/unmute 결과 프리픽스가 "*r AudioMicrophonesMuteResult"가 아니라
"*r MicrophonesMuteResult"("Audio" 없음)라는 걸 실장비 원문으로 확인 — 두 장비
모두 mute가 항상 실패하던 버그(cisco_driver.py `_mute_sync`).
(2) Poly 모델 조회가 connect() 시점 1회만 시도해 실패하면 그 뒤로 영영
"모델 확인 중..."에 머무르던 문제 — 모델을 못 얻었으면 폴링마다 재시도하도록 수정.
(3) Poly mute/dial/hangup이 예상 밖 응답을 조용히 False로 삼켜 원인 진단이
불가능했던 문제 — Cisco처럼 원문을 담아 DriverCommandError로 올리도록 수정.
(4) 프런트엔드가 HTTP 상태만 보고 응답 본문의 {ok:false}를 무시해, 실패했는데도
"완료" 토스트가 뜨던 문제 수정.
(5) 통화 종료 아이콘이 통화 중일 때 항상 빨간색으로 보이도록 변경(기존엔 마우스를
올려야만 빨간색이었음).
1.5.5 = 2026-07-31 VDI 1차 필드 테스트 3차 피드백 — 설정 화면 Teams 테넌트 주소
입력란의 플레이스홀더(예시 텍스트)가 사내 실제 도메인이라 "이미 저장된 값"처럼
보여, 실제로는 비어있는데 채워진 걸로 착각하고 넘어가던 문제. 플레이스홀더를
예시임이 분명한 값(vc.example.com)으로 바꾸고, 값이 비어있으면 경고 문구를
표시하도록 수정. 플레이스홀더 자체도 기울임+옅은 색으로 더 뚜렷하게 구분되도록
CSS 추가. 설정 화면 하단·FastAPI 앱 타이틀에 남아있던 "Codec Control Center"
표기도 "BridgeX"로 정리(2026-07-31 브랜딩 작업에서 누락된 부분).
1.5.6 = 사용자 가이드(/guide)가 글로만 돼 있어 이해하기 어렵다는 피드백 —
실제 대시보드/장비 등록 창/장비 카드/설정 화면 스크린샷 4장을 관련 섹션에
추가. 설정 화면 하단에 제작사(coredxi) 표기가 빠져있어 로고+"coredxi 제작"
문구 추가.
1.5.7 = 가이드 §4 Teams 연동("오늘 예정된 회의", "회의 ID로 직접 참가") 섹션에도
스크린샷 2장 추가. 캡처 과정에서 회의ID(10자리) 입력란이 실제로는 다 입력해도
뒷자리가 스크롤되어 안 보이는 버그를 발견 — `.dial-row .id-input` 너비를
4.6rem에서 5.5rem으로 수정.
1.5.8 = 가이드 화면에서 스크롤 시 헤더/사이드바가 페이지와 함께 스크롤되어 사라지던 문제
재확인 — v1.5.2에서 사이드바만 sticky였다가 화면이 튀는 버그로 sticky를 뺐었는데, 그 결과
헤더와 사이드바가 아예 고정되지 않는 회귀가 남아있었음. `.topbar`도 `position: sticky; top: 0`으로
고정하고, `.sidebar`의 sticky `top`을 `.topbar` 실제 높이(57px, 기존 코드의 53px 가정은 오차가
있었음)로 맞춰 헤더 바로 아래에 처음부터 고정된 위치로 붙여, 스크롤해도 튀지 않으면서 항상
보이도록 수정.
1.5.9 = v1.5.8에서 헤더/사이드바는 고정했지만, 가이드 본문 안의 "목차" 박스는 여전히
가려진 채로 스크롤을 따라가던 문제. 확인해보니 `.guide-toc`는 이미 `position: sticky`였지만
`top` 오프셋이 1.25rem(20px)이라, 새로 sticky가 된 57px짜리 헤더 뒤에 가려진 상태로 붙어있던
것이었음(스크롤 자체는 되고 있었으나 보이지 않았음). `top`을 `57px + 1.25rem`으로 옮겨 헤더
바로 아래에 보이도록 수정.
1.5.10 = VDI 2차 재테스트에서 Poly 장비의 "실제로는 통화중이 아닌데 계속 통화중으로
표시"되는 문제가 재발했다고 재확인됨 — v1.5.3의 장비별 락 수정은 유효했지만 근본 원인의
전부가 아니었음. 직접 확인 결과 실장비는 `callinfo all`에 "system is not in a call"로
정상 응답했는데도(사용자가 별도 클라이언트로 직접 확인) 앱은 계속 통화중으로 표시했고,
관련 오류 로그도 전혀 없었음 — 세션이 한 번이라도 어긋나면(예: 응답 줄 정렬이 밀리는 경우)
`callinfo` 자리에 온 줄이 "system is not in a call"과 다르기만 하면 무조건 in_call=True로
확정해버리던 fail-open 로직이 원인. `_call_block()`에 엄격 검증 모드를 추가해 "callinfo
begin" 블록도 "system is not in a call"도 아닌 응답은 DriverCommandError로 올리도록
수정(poly_driver.py). 이 과정에서 별개의 구조적 문제도 함께 발견: 드라이버가 예외를 던지지
않고 get_status() 내부에서 스스로 삼켜 online=False만 반환하는 경우, PollingScheduler가
연결을 재사용해 계속 같은(깨진) 세션으로 재시도만 하고 영영 재연결하지 않던 문제 —
offline 상태를 받으면 항상 드라이버를 버리고 다음 폴링에서 새로 연결하도록 수정
(polling.py). 추가로 제어 로그 시각이 UTC로 저장·표시돼 실제 한국 시간과 9시간 어긋나
보이던 문제도 함께 수정 — 표시 시점에 KST(UTC+9 고정 오프셋)로 변환(history.py; Windows
실행 환경엔 zoneinfo용 tzdata가 없어 zoneinfo 대신 고정 오프셋 사용).
1.5.11 = v1.5.10 배포 직후 VDI에서 재현: 에러 메시지가 정확히 무엇이었는지 확인한 결과
`unexpected response to 'callinfo all': 'Hi, my name is : 판교 6층 영상회의실'`였음 — Poly
장비가 세션을 새로 열면 명령 요청과 무관하게 인사말 한 줄("Hi, my name is : <장비 이름>")을
스스로 먼저 보내는데, 이게 도착하는 시점이 불규칙해(연결 직후일 수도, 몇 번의 폴링 뒤일 수도
있음) v1.5.10의 엄격 검증에 매번 걸려 재연결해도 반복 실패하고 있었음. `_read_line()` 레벨에서
이 인사말 줄을 만나면 무조건 버리고 다음 줄을 읽도록 수정(poly_driver.py) — 어느 명령의 응답
자리에 끼어들든 투명하게 걸러진다.
1.5.12 = v1.5.11로도 VDI에서 같은 계열 문제가 계속됨 — 이번엔 `unexpected response to
'callinfo all': 'systemsetting get model'`. "systemsetting get model"은 모델을 아직 못 얻었을
때 폴링마다 재전송되는 명령의 문자열 그대로였다. "채널이 방금 보낸 명령을 그대로 에코한다"는
가설로 `_send()`에 보낸 명령을 기록해두고 `_read_line()`에서 같은 문자열이면 버리는 수정을
시도했으나, 시뮬레이터 회귀 테스트에서 바로 깨짐이 드러남 — Poly의 mute on 명령은 정상
성공 응답 자체가 명령과 동일한 문자열("mute near on")이라 이 방식은 정상 응답까지 에코로
오인해 버려버린다(테스트가 없었다면 그대로 배포해 새 버그를 만들 뻔했다). 이 접근은 되돌리고,
대신 문제가 재발했을 때 한 줄짜리 에러 메시지만으로 추측하지 않도록 최근 송수신 원문 몇 줄을
`poly_driver.py`에 남겨뒀다가 검증 실패 시 `logger.warning()`으로 시스템 로그(`/logs`)에
남기도록 추가(진단 강화, 동작 변경 없음) — 다음 재현 시 화면 캡처 대신 로그에서 전체 흐름을
바로 확인할 수 있다. 이번 릴리즈는 근본 수정이 아니라 다음 재현을 정확히 진단하기 위한
계측 추가다.
1.5.13 = v1.5.12에서 추가한 진단 로그로 실제 송수신 원문 트레이스를 여러 건 확보 — 지금까지의
개별 문자열 패치가 왜 계속 부족했는지 드러남. (1) 세션을 새로 열면 나오는 인사말이 "Hi, my name
is : ..." 한 줄이 아니라 "Here is what I know about myself:" 등 몇 줄이 더 이어지는 여러 줄
블록이었음(정확한 줄 수는 문서에 없어 예측 불가) — v1.5.11은 첫 줄만 걸렀던 것. (2) 모든
트레이스에 빈 줄(´´)이 반복적으로 잡음으로 끼어들어 있었음 — 이 드라이버가 다루는 어떤 명령도
빈 줄을 정상 응답으로 준 적이 없다. 이번엔 특정 문자열을 더 추가하는 대신 일반적인 방식으로
수정: connect() 직후 명령을 보내기 전에 짧은 타임아웃으로 반복 읽어 조용해질 때까지 전부
비우는 `_drain_startup_noise_*`를 추가해 인사말이 몇 줄이든 안전하게 흡수하고,
`_read_line()`에서 빈 줄은 항상 잡음으로 간주해 버리도록 함(poly_driver.py). 진단 로그에서
Teams 회의 목록 조회(`calendarmeetings list`)가 mute/callinfo 사이에 끼어드는 것도 관찰됐는데,
락 자체는 걸려 있어 진짜 동시접속은 아니고 이전 호출이 응답을 덜 비운 채 넘어간 잔재로 보임 —
이번 두 수정으로 상당 부분 같이 완화될 것으로 보이나 별도 확정 필요(다음 재현 로그로 확인 예정).
1.5.14 = v1.5.13 배포 후 새 트레이스로 추가 확인: 장비 셸이 "-> " 프롬프트 뒤에 이전에 받은
명령을 그대로 에코해 돌려보내는데, 이게 한두 교환 뒤늦게 나타나 mute/callinfo 응답 자리에
끼어들고 있었음(예: "-> systemsetting get model", "-> calendarmeetings list "today""). 지금까지
"-> "로 시작하는 줄이 실제 데이터였던 적은 한 번도 없어, `_read_line()`에서 이 프리픽스도
무조건 잡음으로 걸러내도록 추가(poly_driver.py).
같은 트레이스에서 `calendarmeetings list "today"`(Teams 회의 목록 조회)의 응답 블록
("calendarmeetings list begin"~"end")이 조각나서 mute/callinfo 응답 자리에 나타나는 것도
확인됨 — 이는 `get_obtp_entries()`가 자신의 응답을 끝까지 기다리지 않고(lenient
`_call_block()`이 begin 마커가 아닌 첫 줄을 그냥 "응답"으로 받아들여버림) 그대로 반환해버려서,
실제 응답이 뒤늦게 도착해 다음 호출들이 대신 주워가는 것으로 보임. 다만 calendarmeetings의
"회의 없음" 실제 응답 형식이 실장비로 확인된 적이 없어(문서/실측 근거 없음) 추측으로 더 좁혀
고치지 않았다 — 다음 VDI 라운드에서 실장비 원문 확보 필요(SCHEDULE.md 참고 대상에 추가할 것).
1.5.15 = v1.5.14의 "calendarmeetings 회의 없음 응답 형식 미확인" 이슈를 사용자가 실장비에
직접 접속해 확보한 원문으로 해소: `calendarmeetings list "today"`에 회의가 없으면
"calendarmeetings list begin" 바로 뒤에 내용 없이 "calendarmeetings list end"가 옴(추측이
아니라 실측 확인). v1.5.14의 "-> " 프롬프트 에코 필터가 이미 이 케이스를 올바르게 처리함을
이 원문 그대로 회귀 테스트로 고정해 확인(코드 변경 없음, `tests/drivers/test_poly_driver.py`).
1.5.16 = v1.5.15 배포 후 새 트레이스로 결정적인 패턴 확인: 응답이 명령보다 정확히 한 교환씩
밀려 들어온다(mute의 진짜 응답이 callinfo 자리에서, model의 진짜 응답이 mute 자리에서 나오는
식 — 같은 모양의 트레이스가 5회 이상 재현). 지금까지는 첫 줄이 기대와 다르면 (callinfo처럼)
곧장 에러로 올리거나 (mute get처럼) 조용히 잘못된 값으로 받아들였는데, 둘 다 근본 대응이
아니었다. `_call_expecting()`을 새로 추가해 응답이 그럴듯한지 확인하고, 아니면 최대 3줄까지
더 읽어 밀려 들어온 진짜 응답을 찾도록 함 — mute get/set, model 조회, uptime 조회, dial,
hangup에 모두 적용(poly_driver.py). 이번 라운드 3번(가짜 통화중 → 계속 오프라인 →
프롬프트 에코)에 걸쳐 부분적으로만 대응했던 "응답 밀림" 문제를 이번에 일반적인 방식으로
근본 해결.
1.5.17 = Teams 연동/기능 체계적 테스트 라운드에서 발견된 버그 2건 수정.
(1) Teams 회의 목록의 링크로 참가하면 Poly 2대 모두 "실패: 200"만 뜨고 원인 불명이었는데
(직접 회의ID 다이얼은 정상) — `join_meeting()`이 `dial()`/`hangup()`/`mute()`와 달리
`_call_expecting()`(응답 밀림 대응)을 안 쓰고 예전 `_call()`을 그대로 쓰고 있던 게 원인.
dial()과 동일하게 수정해 응답이 밀려도 성공을 인식하고, 실패 시엔 원문을 담아 예외로 올리도록
함(예전엔 조용히 False만 반환해 "실패: 200"으로만 보였음).
(2) v1.5.16 배포 후에도 "unexpected response" 오류가 간헐적으로 재발 — `/logs` 원문으로
확인한 결과 세션이 스스로 보내는 자기소개 블록이 "Hi, my name is" 한두 줄이 아니라
Model/Software Version/Build Information/Contact Number/Total Calls/Total Time In
Calls/SNTP Time Service/Local Time is/IP Video Number/MP Enabled/H323 Enabled까지 13줄
넘게 이어지고, 연결 직후가 아니라 세션 중 아무 때나(다음 명령을 받아야 밀린 출력을 내보내는
것으로 추정) 통째로 끼어들 수 있음이 확인됨. 응답 밀림 재시도 한도(`_MAX_LAG_SKIPS`)를
3 → 25로 확대. 모델 정보가 이 자기소개 블록 안에 "Model: 값" 형태로도 나옴을 확인해
`_fetch_model()`이 이 형식도 인식하도록 수정.
1.5.18 = v1.5.17 배포 후에도 Poly Teams 링크 참가가 재현("timeout waiting for device
response"). 실제 원문 확인 결과 `join_meeting()`이 쓰던 `dial phone sip "..."`
(cmd.dial_phone)에 장비가 `info: AUDIO call not enabled`로 응답(거부)하고 있었음 —
응답이 밀린 잡음이 아니라 장비의 진짜 거부 응답이었는데, `_call_expecting`의 재시도
로직이 이를 잡음으로 보고 계속 건너뛰다 결국 응답이 끊겨 타임아웃으로만 보였던 것.
두 가지로 수정(poly_driver.py): (1) join_meeting()이 direct-dial(dial())과 동일하게
검증된 `dial manual`(cmd.dial_manual)을 쓰도록 변경, (2) `_call_expecting()`이 "info: "로
시작하는 줄은 밀림 잡음으로 건너뛰지 않고 곧장 반환하도록 수정 — 앞으로 비슷한 장비 거부
응답은 타임아웃 대신 즉시 원문과 함께 보고된다.
1.5.19 = Teams 연동 라운드 3차 피드백 반영.
(1) **안전 문제**: Cisco 장비에서 통화 중에 회의 링크를 또 클릭하거나 direct-dial을 실행하면
같은 회의에 중복 참가되며 하울링이 발생하는 사고 확인 — 통화 중(`card.dataset.inCall==1`)일
때는 링크 참가/direct-dial을 프런트엔드에서 막고 안내 토스트를 띄우도록 수정
(dashboard.js). 사용자가 실수로 누르는 경우를 막기 위함.
(2) Poly 카드는 통화 중에도 Cisco와 달리 회의 제목이 안 뜨던 문제 — 실장비 원문
("callinfo:1::1330378709@vc.poscodx.com:384:connected:notmuted:outgoing:videocall")으로
확인한 결과 call.id 다음 필드(index 2)가 비어 있고 실제 주소는 index 3에 있는데, 기존 코드는
index 2를 읽고 있어 call_peer가 항상 빈 문자열이었음(그래서 Teams 목록과 매칭이 안 돼 제목이
안 떴음) — index 3으로 수정(poly_driver.py). 시뮬레이터도 이 필드 배치를 반영해 실장비와
맞춤(poly_sim_server.py) — 예전 시뮬레이터는 이 빈 필드 없이 만들어졌던 잘못된 가정이었음.
(3) Poly 회의 참가 후 잠깐 오프라인처럼 보였다가 몇 초 뒤 통화중으로 돌아오는 현상은 이번
라운드에서 추가한 자가복구(응답 밀림 감지 시 오프라인 처리 후 재연결) 메커니즘이 의도대로
동작하는 것으로 보임 — 다음에 재현되면 정확한 메시지를 확보해 재검토.
이후 실장비 검증(Phase③)에서 드러나는 수정은 patch(1.x.y), 기능 추가는 minor(1.x.0)로 올린다.
1.6.0 = 대시보드 사용성 개선 5건 반영(VDI 실사용 피드백) — (1) 그룹 관리(설정 화면에서
그룹 이름 수정/삭제, `/api/groups` 신설) — 이미 존재하는 그룹명으로는 병합 없이 차단,
삭제는 장비의 그룹 태그만 지우고 장비 자체는 유지. (2) 그룹 일괄 제어(Mute/Unmute/재부팅)
버튼 제거 — 오작동 시 여러 대에 동시에 영향을 줄 수 있는 안전 리스크였음(그룹 탭을 통한
필터링/보기는 그대로 유지). (3) "오늘의 예정 회의" 목록에 정렬(시간/회의실/회의명/참가
헤더 클릭)과 페이지네이션(5/10/20개, 이전/다음) 추가 — 정렬 기준·페이지 크기는
localStorage로 다음 방문까지 유지(페이지 번호 자체는 새로고침 시 초기화). (4) 최초 설치
시 다크모드를 기본값으로 변경(기존엔 OS `prefers-color-scheme`를 따름 — 이미 테마를
바꾼 적 있는 사용자의 선택은 그대로 유지). (5) 로그 화면(제어 로그/시스템 로그)에 "전체
복사"·"txt 다운로드" 버튼 추가. 인앱 가이드(`/guide`) 문서·스크린샷도 위 변경사항에 맞게
갱신.
1.6.1 = v1.6.0 VDI 재테스트 피드백 반영. (1) **버그 수정**: Cisco Bookings List/Get이
UTC(Z 접미사)로 내려주는 회의 시간을 그대로 표시하고 있어 실제보다 9시간 이른 시각으로
보이던 문제(예: 실제 13:30 KST 회의가 04:30으로 표시) — `cisco_driver.py`에서 UTC→KST
변환 후 Poly와 동일한 naive 형식으로 맞춤. (2) "오늘의 예정 회의" 표에 시작~종료 시각을
함께 표기(기존엔 시작시간만); 장비 카드별 TEAMS 위젯은 기존대로 시작시간만 유지. (3)
프런트엔드의 "지금(now)" 계산을 UTC 대신 KST 기준으로 맞춰 자정 경계에서도 회의 필터링이
올바르게 동작하도록 수정, 종료된 회의 필터 조건도 단순화. (4) 페이지당 표시 개수(5/10/20)
버튼 관련 문의는 재현 결과 오탐으로 확인(표시할 회의가 선택한 개수보다 적으면 버튼
활성 표시만 바뀌고 목록은 이미 전부 보이는 정상 동작) — 가이드에 설명 추가, 코드 수정
없음. Playwright 기반 종단 회귀 테스트(Cisco 실장비 스킴 포함)로 전 화면 재검증 완료.
1.6.2 = 직원 배포 이후 여러 PC가 같은 장비를 동시에 폴링·제어하는 상황에서, v1.5.19의
"통화 중 중복 참가 방지" 가드가 인스턴스별 폴링 캐시(최대 120초 지연)에만 의존해 다른 PC의
조작을 놓칠 수 있는 레이스 윈도우를 확인 — 재부팅/다이얼/direct-dial/join 실행 직전 서버가
그 순간 장비 상태를 fresh하게 재확인해, 통화 중이면 명령을 보내지 않고 409로 차단하도록
수정(`routes_control.py`, `routes_teams.py`, 신규 `DriverConflictError`). 장비 상태 자체를
확인할 수 없는 경우(오프라인/응답없음)도 "통화 중 아님"으로 오인하지 않도록 동일하게
409로 차단한다(fail-closed). 설계 근거는
docs/superpowers/specs/2026-08-03-multi-instance-control-race-guard-design.md.
1.6.3 = 2026-08-04 VDI 재테스트 중 발견된 버그 2건 수정.
(1) **버그 수정**: Poly 장비의 `callinfo begin`/`callinfo end` 사이가 본문 없이 곧바로
끝나는 경우(원인 미확정 — 통화 상태 전환 중 타이밍으로 추정)에 대비가 없어, 빈 응답을
"통화 중"으로 오판한 뒤 `call_lines[0]`을 읽다 처리되지 않은 IndexError로 폴링 루프가
크래시하던 문제(판교 6층 영상회의실에서 반복 재현, app.log에 "poll raised unexpected
error"로 기록됨) — 다른 세션 뒤섞임 케이스와 동일하게 DriverCommandError로 올려
offline+재연결로 안전하게 처리하도록 수정(poly_driver.py).
(2) **진단 계측 추가**: Cisco 장비의 Teams 회의 목록이 오류 없이 빈 목록만 반환되는
사례(2026-08-04 VDI 재테스트, 판교 6층 B회의실 — 실제로는 캘린더에 회의가 예약돼 있었음)를
재현했으나, Cisco 드라이버는 원래 정상 동작 시 아무 로그도 남기지 않는 구조라 원인을
특정할 수 없었다. `xCommand Bookings List` 응답이 0건으로 파싱될 때 원문을 그대로
경고 로그로 남기도록 추가(cisco_driver.py) — v1.5.12에서 Poly에 적용한 것과 동일한
접근. `routes_teams.py`의 캘린더/OBTP 조회 실패(502)도 이제 원인과 함께 로그에 남는다
(기존엔 조용히 502만 반환). 다음 VDI 재현 시 app.log에서 실제 응답 원문을 확인해
근본 원인을 특정할 예정 — 이번 릴리즈는 근본 수정이 아니라 진단을 위한 계측이다.
1.6.4 = 2026-08-04 VDI 재테스트에서 재현된 "한번 응답불가 상태가 되면 Refresh를
아무리 반복해도 회복되지 않는" 버그의 근본 원인 수정. 판교 6층 영상회의실이 SSH
인증 실패(app.log: "Authentication (password) failed") 상태에 빠진 뒤 재시도해도
계속 같은 실패만 반복됨을 로그로 확인 — 원인은 Poly/Cisco 드라이버 둘 다
`client.connect()`가 실패(인증 실패든 SSHException/OSError든)해도 이미 생성된
`paramiko.SSHClient`를 한 번도 close()하지 않고 버리는 코드였다. 실패한 시도마다
장비 쪽에 미종료 SSH 세션이 하나씩 leak되는 구조라, 장비의 동시 세션 한도가 낮으면
(임베디드 장비 흔한 특성) 재시도(자동 폴링이든 수동 Refresh든) 자체가 leak을 계속
쌓아 상황을 더 악화시킬 뿐 회복되지 않았다. `_connect_ssh_sync`(poly_driver.py)/
`_connect_sync`(cisco_driver.py) 모두 연결 실패 시 client.close()를 반드시 호출하도록
수정(재현 테스트로 leak 확인 후 수정 — TDD). 이 버그가 실제로 최초 인증 실패를
유발한 원인인지(vs 별개 원인으로 시작해 leak이 회복만 막았는지)는 다음 VDI 재테스트로
확인 필요.
1.6.5 = 앱 내장 가이드(`/guide`) 갱신 — 2026-08-04 VDI 재테스트에서 확인된 내용 반영.
(1) v1.6.2의 다중 PC 동시 제어 안전장치가 가이드에 전혀 설명돼 있지 않았다 — "일상 사용"
절에 "여러 PC에서 같은 장비를 관리할 때" 소절을 신설해 재부팅/직접 다이얼/회의 참가가
통화 중이거나 상태 확인 불가 시 차단되는 동작(409 안내 문구 포함)과 음소거/통화종료는
영향받지 않는다는 점을 설명하고, "문제 해결" 절에도 관련 FAQ를 추가했다. (2) 기존
"재부팅" 설명이 "회의실 사용 중 누르면 즉시 통화가 끊긴다"고 돼 있었는데, 이제는 안전
장치가 막아 실제로 끊기지 않는다 — 사실과 다르던 부분 수정. (3) v1.5.1에서 추가한 로컬
포트(8765~8769) 자동 폴백이 가이드에 전혀 언급돼 있지 않아 "시작하기" 절에 추가.
(4) "장비가 계속 오류/오프라인으로 보인다" 항목에 일시적 네트워크 단절 후 자동 재연결
안내 추가(v1.6.4 SSH 세션 leak 수정으로 이제 실제로 스스로 복구됨).
(5) 버전 표기("BridgeX vX.Y.Z · coredxi 제작")를 설정 화면 안에서만 보이던 것을 사이드바
맨 아래(가이드 링크 바로 밑)로 이동 — 모든 화면에서 항상 보이면서도 안 거슬리게, 기존
"가이드" 링크를 사이드바 맨 아래 고정하던 것과 같은 패턴으로 구현. 실제 배치 전 4가지
후보(그대로/사이드바/대시보드 하단/우측 하단 고정)를 목업으로 비교해 사용자가 직접 선택함
— 우측 하단 고정안은 제어 성공/실패 토스트 알림과 같은 자리라 배제. 템플릿 4개
(index/logs/settings/guide.html) 모두 반영, `app_version`은 라우트별 context 대신
`templates.env.globals`로 전역 주입(main.py)하도록 정리.
"""

__version__ = "1.6.5"
