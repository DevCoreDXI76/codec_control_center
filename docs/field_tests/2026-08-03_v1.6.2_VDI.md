# 필드 테스트 계획서 & 체크리스트 — v1.6.2 (VDI 실장비)

- 문서 버전: v1.0
- 작성일: 2026-08-03
- 대상 버전: **v1.6.2** (`BridgeX-v1.6.2.exe`)
- 연관 문서: `PLAN.md`(Phase③ DoD), `PIPELINE.md`(§3 기본 QA 체크리스트, §5 배포), `SCHEDULE.md`,
  `CHANGELOG.md`, `docs/BACKLOG.md`, 이전 라운드 `docs/field_tests/2026-07-31_v1.5.2_VDI.md`(v1.3.0
  라운드는 그 문서의 §3에 이력 보존)
- 배경: v1.5.2 라운드는 v1.5.19까지 이어졌으나, **A~M 체크리스트 중 F(재부팅)/H/I/J/K/L/M
  항목의 "결과" 칸이 끝까지 비어있는 상태로 종료됐다**(확인됨 — 이전 라운드 문서 §2 참고).
  그 사이 직원 배포가 이뤄지면서 여러 PC가 같은 장비를 동시에 폴링·제어하는 상황이 실제로
  발생했고, 이로 인한 안전 문제(다른 PC가 방금 시작한 통화를 놓치고 중복 참가/하울링/오재부팅)
  가능성을 v1.6.2에서 서버사이드 가드로 막았다(설계: `docs/superpowers/specs/2026-08-03-multi-instance-control-race-guard-design.md`).
  이번 라운드는 **① v1.6.2 신규 안전장치 검증을 최우선으로**, ② v1.5.2 라운드에서 비어있던
  F/H/I/J/K/L/M을 마무리, ③ 그 사이 나온 v1.6.0/v1.6.1 변경사항 검증까지 함께 처리한다.
- **이 파일은 실행하면서 그 자리에서 채워 넣는 기록지다.**
- **2026-08-03 갱신**: 문서만 오늘 작성 완료, 실제 VDI 실행은 **2026-08-04(예정)**로 확정.

---

## 0. 테스트 환경 기록

| 항목 | 내용 |
|---|---|
| 테스트 일시 | 2026-08-04(예정) ( : ~ : ) |
| 테스터 | |
| VDI 세션 정보 | |
| exe 파일/버전 확인 | `BridgeX-v1.6.2.exe`, SHA/빌드 시각: |
| 대상 장비 목록 | 아래 참고(현재 등록된 실장비로 갱신) |

**대상 장비 상세** (v1.5.2 라운드 기준, 실제 등록 현황에 맞게 행 추가/수정):

| # | 제조사 | 모델 | 연결방식 | IP/호스트 | 비고 |
|---|---|---|---|---|---|
| 1 | Poly | RealPresence Group 700 | SSH | 203.238.221.36 | 판교 6층 영상회의실 |
| 2 | Cisco | Webex Room Bar | SSH | 192.168.62.51 | 판교 6층 B회의실 |
| 3 | Poly | RealPresence Group 700 | SSH | 203.238.221.26 | 포항 중회의실 |

**A절(다중 PC 안전장치) 테스트에 필요한 준비물**:
- [ ] 같은 장비를 등록해 폴링할 **PC 2대(또는 브라우저 2개 세션)** — 가능하면 실제로 서로
  다른 PC에서 각자 exe를 띄우는 것이 가장 현실적이나, 여의치 않으면 같은 PC에서 브라우저
  탭 2개로 같은 대시보드를 띄워도 A절 시나리오 검증에는 충분하다(가드가 서버에서 장비의
  실제 상태를 직접 재확인하는 방식이라, 요청이 어느 탭/PC에서 왔는지는 무관함).

---

## 1. 사전 준비

- [ ] `pytest -q` 전체 통과 확인(이미 완료 — 336개, main 병합 커밋 `5e0739f` 기준)
- [ ] 최신 exe(`BridgeX-v1.6.2.exe`)가 VDI에 반입되어 있고 이전 exe는 별도 위치에 보관됨
- [ ] `data/` 폴더가 이전 버전과 동일한 위치를 그대로 사용함(장비 목록/설정 유지 확인용)

---

## 2. 테스트 케이스

범례 — **상태**: `신규` = 이번 라운드에 처음 확인, `이월` = 이전 라운드에서 결과 미기입으로
넘어온 항목, `회귀` = 이전에 확인된 항목의 재확인. **결과**: Pass / Fail / Skip.

### A. 다중 PC 인스턴스 동시 제어 안전장치 (v1.6.2 신규, **최우선**)

배경: `reject_if_in_call()`(`app/api/routes_control.py`)이 재부팅·다이얼·direct-dial·참가
직전에 장비의 실제 상태를 fresh하게 재확인해, ① 이미 통화 중이거나 ② 장비 상태를 확인할 수
없을 때(응답없음/타임아웃) 모두 명령을 차단(409)한다. mute/unmute/hangup은 대상이 아니다.

| ID | 항목 | 절차 | 기대 결과 | 상태 | 결과 | 비고/로그 |
|---|---|---|---|---|---|---|
| A1 | 통화 중 재부팅 차단 | PC/탭 ①에서 장비를 통화 중으로 만든다(direct-dial 등) → PC/탭 ②에서 같은 장비의 재부팅 버튼 클릭·확인 | 재부팅이 **실행되지 않고** "다른 위치에서 이미 통화 중입니다 — 종료 후 다시 시도해주세요" 토스트, 실제 장비도 재부팅 안 됨 | 신규 | | `/logs`에 reboot 실패(success=false) 기록 확인 |
| A2 | 통화 중 다이얼 차단 | 통화 중인 장비에 PC/탭 ②에서 회의ID 수동 다이얼(direct-dial) 시도 | "이미 통화 중입니다" 계열 실패, 실제로 다이얼 안 됨(중복 참가 없음) | 신규 | | |
| A3 | 통화 중 회의 링크 참가 차단 | 통화 중인 장비의 "오늘의 예정 회의" 링크를 PC/탭 ②에서 클릭 | 참가 명령이 실제로 전송되지 않고 실패 토스트만 표시(하울링 재현 안 됨) | 신규 | | v1.5.19에서 문제됐던 정확히 그 시나리오 — 반드시 확인 |
| A4 | 통화 중이어도 음소거/종료는 정상 동작 | 통화 중인 장비에서 mute/unmute, hangup 실행 | 두 동작 모두 정상 실행됨(A1~A3와 달리 차단되지 않아야 함) | 신규 | | 가드 범위가 딱 4개 명령(재부팅/다이얼/direct-dial/참가)에만 걸려있는지 확인하는 음성 테스트 |
| A5 | 장비 응답 불가 상태에서 명령 차단 | 장비 네트워크를 잠깐 끊거나(케이블/스위치 포트) 방화벽에서 SSH/Telnet 포트를 일시 차단한 뒤, 그 상태에서 재부팅/다이얼 시도 | "장비 상태를 확인할 수 없어 명령을 보내지 않았습니다" 실패, 실제 명령 전송 안 됨 | 신규 | | 재현이 번거로우면 Skip 가능(코드로는 보장되나 실장비 타이밍은 미검증) — 가능하면 꼭 1회 확인 |
| A6 | 정상 상태에서는 그대로 동작 | 통화 중이 아니고 응답도 정상인 장비에서 재부팅/다이얼/참가 각각 실행 | 모두 평소처럼 정상 실행(이번 변경으로 정상 경로가 막히지 않았는지 회귀 확인) | 회귀 | | |

### B. 이월 — v1.5.2 라운드에서 결과 미기입 (F/H/I/J/K/L/M)

이전 라운드에서 상태만 "신규"로 남고 결과 칸이 끝까지 비어 있던 항목이다. 세부 절차는
`docs/field_tests/2026-07-31_v1.5.2_VDI.md` §2의 해당 절을 그대로 따르되, 여기 표에
결과만 옮겨 적어도 되고 필요하면 절차를 다시 옮겨써도 된다.

| 절 | 항목 요약 | 상태 | 결과 | 비고/로그 |
|---|---|---|---|---|
| F1 | Reboot 확인 다이얼로그 표시/취소 동작 | 이월 | | |
| F2 | Reboot 실제 실행(고위험 — 회의실 미사용 시간대에만) | 이월 | | **실행 시각 기록 필수**: |
| H1~H4 | 수정 모달, 모달 내 삭제, 설정값 재시작 후 유지, Teams 테넌트 주소 우선순위 | 이월 | | |
| I1~I4 | `/logs` 탭 전환, 시스템 로그 내용, 로그 로테이션, 제어→로그 반영 | 이월 | | |
| J1~J2 | 앱 재시작 후 데이터 유지, exe만 교체 시 data/ 유지 | 이월 | | |
| K1~K5 | 브랜드 로고/파비콘/exe 아이콘/다크모드 가독성/exe 파일명 | 이월 | | |
| L1~L5 | 인앱 가이드 섹션/목차/내용 정확성/스크롤 | 이월 | | |
| M1~M3 | 포트 충돌 자동 재시도(기본/폴백/전부충돌) | 이월 | | |

### C. v1.6.0 대시보드 사용성 개선 검증 (미검증 상태로 배포됨)

| ID | 항목 | 절차 | 기대 결과 | 상태 | 결과 | 비고/로그 |
|---|---|---|---|---|---|---|
| C1 | 그룹 이름 수정/삭제 | 설정 화면에서 그룹 이름 변경 → 삭제 | 이름 변경 반영, 삭제 시 장비는 유지되고 그룹 태그만 제거 | 신규 | | 이미 존재하는 이름으로 변경 시 차단되는지도 확인 |
| C2 | 그룹 일괄 제어 버튼이 없음 확인 | 그룹 탭에서 일괄 Mute/재부팅 버튼 존재 여부 확인 | 버튼이 보이지 않음(v1.6.0에서 안전상 제거됨), 필터링(보기)만 동작 | 신규 | | |
| C3 | 오늘의 예정 회의 정렬/페이지네이션 | 시간/회의실/회의명/참가 헤더 클릭, 5/10/20 페이지 크기 전환 | 정렬 동작, 페이지 크기 변경이 재방문 시에도 유지(localStorage) | 신규 | | |
| C4 | 로그 화면 전체복사/다운로드 | `/logs`에서 전체 복사, txt 다운로드 버튼 클릭 | 클립보드 복사 및 파일 다운로드 정상 동작 | 신규 | | |
| C5 | 최초 설치 시 다크모드 기본값 | `data/` 삭제 후(또는 신규 VDI 세션) 최초 실행 | 다크모드가 기본으로 켜져 있음 | 신규 | | 기존 사용자의 테마 선택은 유지되는지도 함께 확인 |

### D. v1.6.1 Cisco 시간대 표기 수정 검증

| ID | 항목 | 절차 | 기대 결과 | 상태 | 결과 | 비고/로그 |
|---|---|---|---|---|---|---|
| D1 | Cisco 회의 시각 KST 정상 표시 | Cisco 장비의 오늘 회의 목록/카드 확인 | 실제 회의 시각(KST) 그대로 표시(UTC로 9시간 밀리지 않음) | 신규 | | v1.6.0까지는 13:30 회의가 04:30으로 보이던 버그 |
| D2 | 시작~종료 시각 표기 | "오늘의 예정 회의" 표에서 회의 항목 확인 | 시작~종료 시각이 함께 표시됨(카드별 위젯은 시작시간만) | 신규 | | |
| D3 | 자정 경계 필터링 | 자정 근처 시간대에 회의 목록 확인(가능한 경우) | 종료된 회의가 KST 기준으로 올바르게 제외됨 | 신규 | | 재현이 어려우면 Skip 가능 |

### E. 재관찰 대상 (이전 라운드에서 미확정으로 남은 이슈)

| ID | 내용 | 절차 | 확인할 것 |
|---|---|---|---|
| I-5 | Poly direct-dial 연속 실행 시 실패 오기록 여부 | 짧은 시간 안에 direct-dial 연속 실행 | `/logs`에 실패로 잘못 기록되는지, 원문이 남는지 |
| I-9 | Poly SSH 인증 반복 실패 원인 | 자연 발생 관찰(재현 절차 없음) — 응답 밀림 재발 시 함께 관찰 | 짧은 간격 반복 재연결/인증 실패가 계속되는지 |

---

## 3. 발견 이슈 로그

새 이슈가 생길 때마다 아래 표에 행을 추가한다.

| ID | 관련 테스트 케이스 | 증상 | 재현 절차 | 로그(`data/app.log` 발췌) | 심각도 | 후속 조치 |
|---|---|---|---|---|---|---|
| | | | | | | |

---

## 4. Phase③ DoD 판정 (PLAN.md 기준)

> "최소 1대 이상의 실제 Poly, Cisco 장비에서 상태 조회 및 기본 제어(mute 등 저위험 명령부터)가
> 정상 동작함을 확인. 보안팀 관점에서 이상 트래픽 미발생."

- [x] Poly 실장비 ≥1대 상태조회+기본제어 정상 (v1.5.2 라운드에서 이미 확인됨)
- [x] Cisco 실장비 ≥1대 상태조회+기본제어 정상 (v1.5.2 라운드에서 이미 확인됨)
- [ ] 이상 트래픽/보안 오탐 없음(C4, 2026-07-29 1차 확인 — 다중 PC 시나리오는 재확인 필요,
      해당 논의 별도 진행 중)
- [ ] **A~E 전체 항목 결과 기입 완료** → 완료 시 Phase③ DoD 잔여 항목 충족으로 판정
- [ ] 미충족 항목 있음 → §3 이슈 정리 후 재테스트 라운드 필요

## 5. 다음 액션

- [ ] A절(다중 PC 안전장치) 결과를 근거로 안전장치가 실제로 유효한지 최종 확인 — 문제 있으면
      즉시 최우선 수정 대상
- [ ] B절(F/H/I/J/K/L/M)까지 전부 채워지면 Phase③ DoD 최종 판정
- [ ] 이 라운드 결과를 `SCHEDULE.md`/`docs/BACKLOG.md`/Notion 업무 DB에 반영
- [ ] 발견된 이슈는 §3에 기록 후 `PIPELINE.md` 절차(자동테스트→QA→버전/릴리즈)로 수정
