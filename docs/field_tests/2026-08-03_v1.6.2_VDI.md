# 필드 테스트 계획서 & 체크리스트 — v1.6.2 (VDI 실장비)

- 문서 버전: v1.0
- 작성일: 2026-08-03
- 대상 버전: **v1.6.2** (`BridgeX-v1.6.2.exe`)
- 연관 문서: `PLAN.md`(Phase③ DoD), `PIPELINE.md`(§3 기본 QA 체크리스트, §5 배포), `SCHEDULE.md`,
  `CHANGELOG.md`, `docs/BACKLOG.md`, 이전 라운드 `docs/field_tests/2026-07-31_v1.5.2_VDI.md`(v1.3.0
  라운드는 그 문서의 §3에 이력 보존)
- 배경: v1.5.2 라운드는 v1.5.19까지 이어졌으나, **A~M 체크리스트 중 F(재부팅)/H/I/J/K/L/M
  항목의 "결과" 칸이 끝까지 비어있는 상태로 종료됐다**(확인됨 — 이전 라운드 문서 §2 참고).
  그 사이 직원 배포가 이뤄지면서 여러 PC가 같은 장비를 동시에 폴링·제어하는 상황이 실제로
  발생했고, 이로 인한 안전 문제(다른 PC가 방금 시작한 통화를 놓치고 중복 참가/하울링/오재부팅)
  가능성을 v1.6.2에서 서버사이드 가드로 막았다(설계: `docs/superpowers/specs/2026-08-03-multi-instance-control-race-guard-design.md`).
  이번 라운드는 **① v1.6.2 신규 안전장치 검증을 최우선으로**, ② v1.5.2 라운드에서 비어있던
  F/H/I/J/K/L/M을 마무리, ③ 그 사이 나온 v1.6.0/v1.6.1 변경사항 검증까지 함께 처리한다.
- **이 파일은 실행하면서 그 자리에서 채워 넣는 기록지다.**
- **2026-08-03 갱신**: 문서만 오늘 작성 완료, 실제 VDI 실행은 **2026-08-04(예정)**로 확정.
- **2026-08-04 실행 완료**: A~D 전체 Pass 보고. 다만 같은 세션 중 이슈 2건(§3 ISSUE-1/2)이
  실제로 발생해 v1.6.3(진단 로깅)/v1.6.4(SSH 세션 leak 수정)를 당일 추가 배포함.
- **2026-08-04 재검증 완료**: v1.6.4 exe로 ISSUE-1/2 둘 다 실장비에서 정상 동작 재확인 —
  **Phase③ DoD 최종 충족 판정**(§4). ISSUE-1 근본 원인은 미확정으로 남았으나 재현되지 않아
  차단 사유는 아님.

---

## 0. 테스트 환경 기록

| 항목 | 내용 |
|---|---|
| 테스트 일시 | 2026-08-04 ( 09:20 ~ : ) |
| 테스터 | 피프티바이브 |
| VDI 세션 정보 | |
| exe 파일/버전 확인 | `BridgeX-v1.6.2.exe`, SHA256: `8ab3df2e5828bc3dc3f13eb2186743c1e10ee6f2c3001cb8f628b4b7b0eebba9`, 빌드 시각: 2026-08-04 07:45:36 |
| 대상 장비 목록 | 아래 참고(현재 등록된 실장비로 갱신) |

**대상 장비 상세** (2026-08-04 테스트 시점 실제 등록 현황):

| # | 제조사 | 모델 | 연결방식 | IP/호스트 | 비고 |
|---|---|---|---|---|---|
| 1 | Poly | RealPresence Group 700 | SSH | 203.238.221.36 | 판교 6층 영상회의실 |
| 2 | Cisco | Webex Room Bar | SSH | 192.168.62.51 | 판교 6층 B회의실 |
| 3 | Poly | RealPresence Group 700 | SSH | 203.238.221.26 | 포항 중회의실 |
| 4 | Poly | RealPresence Group 500 | SSH | 203.238.221.47 | IT사업실장실 |
| 5 | Cisco | Webex Room Bar | SSH | 192.168.92.51 | 판교 9층 B회의실 |

**A절(다중 PC 안전장치) 테스트에 필요한 준비물**:
- [ ] 같은 장비를 등록해 폴링할 **PC 2대(또는 브라우저 2개 세션)** — 가능하면 실제로 서로
  다른 PC에서 각자 exe를 띄우는 것이 가장 현실적이나, 여의치 않으면 같은 PC에서 브라우저
  탭 2개로 같은 대시보드를 띄워도 A절 시나리오 검증에는 충분하다(가드가 서버에서 장비의
  실제 상태를 직접 재확인하는 방식이라, 요청이 어느 탭/PC에서 왔는지는 무관함).

---

## 1. 사전 준비

- [x] `pytest -q` 전체 통과 확인(이미 완료 — 336개, main 병합 커밋 `5e0739f` 기준)
- [x] 최신 exe(`BridgeX-v1.6.2.exe`)가 VDI에 반입되어 있고 이전 exe는 별도 위치에 보관됨
- [x] `data/` 폴더가 이전 버전과 동일한 위치를 그대로 사용함(장비 목록/설정 유지 확인용)

---

## 2. 테스트 케이스

범례 — **상태**: `신규` = 이번 라운드에 처음 확인, `이월` = 이전 라운드에서 결과 미기입으로
넘어온 항목, `회귀` = 이전에 확인된 항목의 재확인. **결과**: Pass / Fail / Skip.

### A. 다중 PC 인스턴스 동시 제어 안전장치 (v1.6.2 신규, **최우선**)

배경: `reject_if_in_call()`(`app/api/routes_control.py`)이 재부팅·다이얼·direct-dial·참가
직전에 장비의 실제 상태를 fresh하게 재확인해, ① 이미 통화 중이거나 ② 장비 상태를 확인할 수
없을 때(응답없음/타임아웃) 모두 명령을 차단(409)한다. mute/unmute/hangup은 대상이 아니다.

| ID | 항목 | 절차 | 기대 결과 | 상태 | 결과 | 비고/로그 |
|---|---|---|---|---|---|---|
| A1 | 통화 중 재부팅 차단 | PC/탭 ①에서 장비를 통화 중으로 만든다(direct-dial 등) → PC/탭 ②에서 같은 장비의 재부팅 버튼 클릭·확인 | 재부팅이 **실행되지 않고** "다른 위치에서 이미 통화 중입니다 — 종료 후 다시 시도해주세요" 토스트, 실제 장비도 재부팅 안 됨 | 신규 | Pass | `/logs`에 reboot 실패(success=false) 기록 확인함 — `2026-08-04 09:39:43 판교 6층 B회의실 reboot 실패: 다른 위치에서 이미 통화 중입니다 — 종료 후 다시 시도해주세요` |
| A2 | 통화 중 다이얼 차단 | 통화 중인 장비에 PC/탭 ②에서 회의ID 수동 다이얼(direct-dial) 시도 | "이미 통화 중입니다" 계열 실패, 실제로 다이얼 안 됨(중복 참가 없음) | 신규 | Pass | |
| A3 | 통화 중 회의 링크 참가 차단 | 통화 중인 장비의 "오늘의 예정 회의" 링크를 PC/탭 ②에서 클릭 | 참가 명령이 실제로 전송되지 않고 실패 토스트만 표시(하울링 재현 안 됨) | 신규 | Pass | v1.5.19에서 문제됐던 정확히 그 시나리오 — 반드시 확인 |
| A4 | 통화 중이어도 음소거/종료는 정상 동작 | 통화 중인 장비에서 mute/unmute, hangup 실행 | 두 동작 모두 정상 실행됨(A1~A3와 달리 차단되지 않아야 함) | 신규 | Pass | 가드 범위가 딱 4개 명령(재부팅/다이얼/direct-dial/참가)에만 걸려있는지 확인하는 음성 테스트 |
| A5 | 장비 응답 불가 상태에서 명령 차단 | 장비 네트워크를 잠깐 끊거나(케이블/스위치 포트) 방화벽에서 SSH/Telnet 포트를 일시 차단한 뒤, 그 상태에서 재부팅/다이얼 시도 | "장비 상태를 확인할 수 없어 명령을 보내지 않았습니다" 실패, 실제 명령 전송 안 됨 | 신규 | Pass | 재현이 번거로우면 Skip 가능(코드로는 보장되나 실장비 타이밍은 미검증) — 가능하면 꼭 1회 확인 |
| A6 | 정상 상태에서는 그대로 동작 | 통화 중이 아니고 응답도 정상인 장비에서 재부팅/다이얼/참가 각각 실행 | 모두 평소처럼 정상 실행(이번 변경으로 정상 경로가 막히지 않았는지 회귀 확인) | 회귀 | Pass | |

### B. 이월 — v1.5.2 라운드에서 결과 미기입 (F/H/I/J/K/L/M)

이전 라운드에서 상태만 "신규"로 남고 결과 칸이 끝까지 비어 있던 항목이다. 세부 절차는
`docs/field_tests/2026-07-31_v1.5.2_VDI.md` §2의 해당 절을 그대로 따르되, 여기 표에
결과만 옮겨 적어도 되고 필요하면 절차를 다시 옮겨써도 된다.

| 절 | 항목 요약 | 상태 | 결과 | 비고/로그 |
|---|---|---|---|---|
| F1 | Reboot 확인 다이얼로그 표시/취소 동작 | 이월 | Pass | |
| F2 | Reboot 실제 실행(고위험 — 회의실 미사용 시간대에만) | 이월 | Pass | **실행 시각 기록 필수**: 09:45 |
| H1~H4 | 수정 모달, 모달 내 삭제, 설정값 재시작 후 유지, Teams 테넌트 주소 우선순위 | 이월 | Pass | |
| I1~I4 | `/logs` 탭 전환, 시스템 로그 내용, 로그 로테이션, 제어→로그 반영 | 이월 | Pass | |
| J1~J2 | 앱 재시작 후 데이터 유지, exe만 교체 시 data/ 유지 | 이월 | Pass | |
| K1~K5 | 브랜드 로고/파비콘/exe 아이콘/다크모드 가독성/exe 파일명 | 이월 | Pass | |
| L1~L5 | 인앱 가이드 섹션/목차/내용 정확성/스크롤 | 이월 | Pass | |
| M1~M3 | 포트 충돌 자동 재시도(기본/폴백/전부충돌) | 이월 | Pass | |

### C. v1.6.0 대시보드 사용성 개선 검증 (미검증 상태로 배포됨)

| ID | 항목 | 절차 | 기대 결과 | 상태 | 결과 | 비고/로그 |
|---|---|---|---|---|---|---|
| C1 | 그룹 이름 수정/삭제 | 설정 화면에서 그룹 이름 변경 → 삭제 | 이름 변경 반영, 삭제 시 장비는 유지되고 그룹 태그만 제거 | 신규 | Pass | 이미 존재하는 이름으로 변경 시 차단되는지도 확인 |
| C2 | 그룹 일괄 제어 버튼이 없음 확인 | 그룹 탭에서 일괄 Mute/재부팅 버튼 존재 여부 확인 | 버튼이 보이지 않음(v1.6.0에서 안전상 제거됨), 필터링(보기)만 동작 | 신규 | Pass | |
| C3 | 오늘의 예정 회의 정렬/페이지네이션 | 시간/회의실/회의명/참가 헤더 클릭, 5/10/20 페이지 크기 전환 | 정렬 동작, 페이지 크기 변경이 재방문 시에도 유지(localStorage) | 신규 | Pass | |
| C4 | 로그 화면 전체복사/다운로드 | `/logs`에서 전체 복사, txt 다운로드 버튼 클릭 | 클립보드 복사 및 파일 다운로드 정상 동작 | 신규 | Pass | |
| C5 | 최초 설치 시 다크모드 기본값 | `data/` 삭제 후(또는 신규 VDI 세션) 최초 실행 | 다크모드가 기본으로 켜져 있음 | 신규 | Pass | 기존 사용자의 테마 선택은 유지되는지도 함께 확인 |

### D. v1.6.1 Cisco 시간대 표기 수정 검증

| ID | 항목 | 절차 | 기대 결과 | 상태 | 결과 | 비고/로그 |
|---|---|---|---|---|---|---|
| D1 | Cisco 회의 시각 KST 정상 표시 | Cisco 장비의 오늘 회의 목록/카드 확인 | 실제 회의 시각(KST) 그대로 표시(UTC로 9시간 밀리지 않음) | 신규 | Pass | v1.6.0까지는 13:30 회의가 04:30으로 보이던 버그. 같은 세션 중 판교 6층 B회의실에서 별도로 "목록 자체가 빈 상태" 이슈(§3 ISSUE-1)가 있었으나 2026-08-04 v1.6.4 재검증에서 정상 표시 확인됨 — D1 판정과 상충 해소 |
| D2 | 시작~종료 시각 표기 | "오늘의 예정 회의" 표에서 회의 항목 확인 | 시작~종료 시각이 함께 표시됨(카드별 위젯은 시작시간만) | 신규 | Pass | |
| D3 | 자정 경계 필터링 | 자정 근처 시간대에 회의 목록 확인(가능한 경우) | 종료된 회의가 KST 기준으로 올바르게 제외됨 | 신규 | Pass | 재현이 어려우면 Skip 가능 |

### E. 재관찰 대상 (이전 라운드에서 미확정으로 남은 이슈)

| ID | 내용 | 절차 | 확인할 것 |
|---|---|---|---|
| I-5 | Poly direct-dial 연속 실행 시 실패 오기록 여부 | 짧은 시간 안에 direct-dial 연속 실행 | `/logs`에 실패로 잘못 기록되는지, 원문이 남는지 |
| I-9 | Poly SSH 인증 반복 실패 원인 | 자연 발생 관찰(재현 절차 없음) — 응답 밀림 재발 시 함께 관찰 | 짧은 간격 반복 재연결/인증 실패가 계속되는지 |

**2026-08-04 갱신**: I-9이 이번 라운드에서 실제로 자연 재현됨(판교 6층 영상회의실, 09:39~09:41 —
`Authentication (password) failed`가 Refresh를 반복해도 계속 반복됨). 원인 특정 완료 + 수정
완료 — §3 ISSUE-2 참고. I-5는 이번 라운드 결과에 별도 언급 없음 — 미확인 상태 유지.

---

## 3. 발견 이슈 로그

새 이슈가 생길 때마다 아래 표에 행을 추가한다.

| ID | 관련 테스트 케이스 | 증상 | 재현 절차 | 로그(`data/app.log` 발췌) | 심각도 | 후속 조치 |
|---|---|---|---|---|---|---|
| ISSUE-1 | D1 (판교 6층 B회의실) | Teams 회의 목록이 빈 채로 표시됨 — Outlook 캘린더에 오늘 실제 예약된 회의가 있는데도 카드에 안 뜸. 시간 포맷 문제(D1 취지)가 아니라 목록 자체가 비어있는 것 | 판교 6층 B회의실 카드에서 "오늘의 예정 회의" 확인 | Cisco 드라이버가 원래 정상 동작 시 로그를 남기지 않아 원인 로그 확보 실패. `xCommand Bookings List` 응답이 0건으로 파싱될 때 원문을 경고 로그로 남기는 진단 계측을 v1.6.3에 추가함(cisco_driver.py) | 중 | **재검증 완료(2026-08-04, v1.6.4)**: 판교 6층 B회의실 Teams 목록 정상 표시 확인됨. **단, 근본 원인은 여전히 미확정** — v1.6.3은 진단 로깅만 추가했을 뿐 코드 동작을 바꾸지 않았는데도 이번엔 정상 동작함(진단 로그 원문도 확보 못함). 최초 증상이 일시적이었는지, 다른 무언가로 우연히 해소됐는지 불명 — 재발하면 진단 로그부터 확인할 것 |
| ISSUE-2 | I-9 / A5 인접 | 판교 6층 영상회의실이 한번 SSH 인증 실패 상태가 되자 Refresh를 반복해도 회복되지 않음(`Authentication (password) failed` 반복). 09:37~09:41 사이 세션 밀림(`implausible line`) → 인증 실패 반복 → `IndexError`로 폴링 루프 크래시(`callinfo begin`/`end` 사이 빈 응답)까지 함께 관찰됨 | 자연 발생(재현 절차 없음) | `app.log` 09:37:06~09:41:09 구간 — `poll failed: Authentication failed` 반복, `IndexError: list index out of range`(poly_driver.py:424), `ConnectionResetError [WinError 10054]` | 상 | **재검증 완료(2026-08-04, v1.6.4)**: 판교 6층 영상회의실 재부팅 후 자동으로 온라인 복귀 + Teams 목록 정상 확인 — SSH 세션 leak 수정이 유효한 것으로 판단. 원 시나리오(인증실패 유발 후 Refresh 반복)와 100% 동일한 재현은 아니고 재부팅 경로로 확인한 것이나, leak 자체가 해소됐으므로 결론은 동일 |

---

## 4. Phase③ DoD 판정 (PLAN.md 기준)

> "최소 1대 이상의 실제 Poly, Cisco 장비에서 상태 조회 및 기본 제어(mute 등 저위험 명령부터)가
> 정상 동작함을 확인. 보안팀 관점에서 이상 트래픽 미발생."

- [x] Poly 실장비 ≥1대 상태조회+기본제어 정상 (v1.5.2 라운드에서 이미 확인됨)
- [x] Cisco 실장비 ≥1대 상태조회+기본제어 정상 (v1.5.2 라운드에서 이미 확인됨)
- [x] 이상 트래픽/보안 오탐 없음 (2026-08-04 v1.6.4 재검증 라운드까지 포함해 보안 오탐 보고
      없음 — 다만 사용량 증가 시 보안팀과 별도 협의는 여전히 조건부로 열려있음, SCHEDULE.md
      §4 참고)
- [x] **A~D 전체 항목 결과 기입 완료** (E는 확인 항목 성격상 결과란 없음 — I-9는 §3 ISSUE-2로
      전환, I-5는 이번 라운드 미확인 상태 유지)
- [x] **모든 미충족 항목 재검증 완료(2026-08-04, v1.6.4)** — ISSUE-1(Cisco Teams 목록 빈 표시)·
      ISSUE-2(Poly SSH 인증 실패 후 미회복) 둘 다 실장비에서 정상 동작 재확인됨(§3 참고).
      **Phase③ DoD 충족으로 최종 판정한다.** 단, ISSUE-1은 근본 원인이 끝내 미확정 상태로
      종결됐다는 점은 남겨둔다(재발 시 진단 로그로 원인 특정 예정) — I-5(direct-dial 연속
      실행)도 이번 라운드에서 확인되지 않아 재관찰 대상으로 유지.

## 5. 다음 액션

- [x] A절(다중 PC 안전장치) 결과를 근거로 안전장치가 실제로 유효한지 최종 확인 — 전부 Pass,
      문제 없음
- [x] B절(F/H/I/J/K/L/M)까지 전부 채워짐, §4 판정 완료
- [x] **v1.6.4 재검증 완료**: 판교 6층 영상회의실 재부팅 후 자동 온라인 복귀 + Teams 목록 정상
      (ISSUE-2 해소 확인). 판교 6층 B회의실 Teams 목록도 정상 표시 확인(ISSUE-1 재현 안 됨,
      근본 원인은 미확정으로 남음)
- [x] 이 라운드 결과를 `SCHEDULE.md`/`docs/PLAN.md`/프로젝트 `CLAUDE.md`에 반영 (2026-08-04)
- [x] 발견된 이슈는 §3에 기록, `PIPELINE.md` 절차대로 v1.6.3(진단)/v1.6.4(수정) 배포 및
      재검증까지 완료
- [ ] I-5(Poly direct-dial 연속 실행 시 오기록 여부)는 계속 미확인 — 다음 라운드에서 관찰
- [ ] ISSUE-1 근본 원인 미확정 — 재발 시 `app.log`의 "cisco bookings list parsed to 0
      entries" 진단 로그로 원인 특정
- [ ] Phase④(Teams 네이티브 OBTP 참가 실장비 검증) 착수 — SCHEDULE.md M3 참고
